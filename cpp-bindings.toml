# Configure the Sword library checkout and build settings.
# This file is portable and stays with the repository.

git_url = "https://github.com/Edcorp844/sword.git"
git_rev = "HEAD"
header = "include/sword/flatapi.h"
lib_name = "sword"
use_cmake = true
link_kind = "static"

# --- AUTOMATED INTEGRATION DEPENDENCIES ---
curl_ios_toolchain_url = "https://github.com/jasonacox/Build-OpenSSL-cURL.git"